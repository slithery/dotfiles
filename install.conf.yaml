- clean: ['~']

- shell: [mkdir -p ~/.config ~/.cache/vim]

- link:
    ~/.config/bash: bash/
    ~/.config/git: git/
    ~/.config/modprobed-db.conf: modprobed-db.conf
    ~/.config/vim: vim/
    ~/.config/xorg: xorg/

